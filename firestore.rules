
rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Allow public read/write access to all collections for simplicity.
    // This is not recommended for production apps with sensitive data.
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
